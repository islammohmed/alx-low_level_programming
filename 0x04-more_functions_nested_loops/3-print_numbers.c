#include "main.h"

/**
 * print_numbers -  a function that print number from 0 to 9
 * Return: the numbers
 */
void print_numbers(void)
{
int i = 0;
for (i = 0; i <= 9; i++)
printf("%d", i);
printf("\n");
}
